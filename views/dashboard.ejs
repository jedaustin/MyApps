<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - WebLauncher</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css" rel="stylesheet">
    <link href="/css/style.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/dashboard">
                <i class="fas fa-link me-2"></i>WebLauncher
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <span class="navbar-text me-3">
                            <i class="fas fa-user me-1"></i><%= user.name || user.username %>
                        </span>
                    </li>
                    <li class="nav-item">
                        <button class="btn btn-outline-light btn-sm" onclick="handleLogout()">
                            <i class="fas fa-sign-out-alt me-1"></i>Logout
                        </button>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <div id="pullToRefreshIndicator" class="pull-to-refresh-indicator" aria-hidden="true">
            <i class="fas fa-arrow-down me-2"></i><span>Pull to refresh</span>
        </div>

        <div class="dashboard-header mb-3 g-3">
            <div class="dashboard-title">
                <h2 class="mb-0">My URLs</h2>
                <button
                    class="btn btn-link p-0 d-flex align-items-center gap-2 d-md-none manage-categories-link"
                    type="button"
                    onclick="openManageCategoriesModal()"
                    aria-label="Manage categories"
                >
                    <i class="fas fa-tags"></i><span>Manage Categories</span>
                </button>
            </div>
            <div class="dashboard-search">
                <div class="input-group search-bar">
                    <span class="input-group-text">
                        <i class="fas fa-search"></i>
                    </span>
                    <input
                        type="search"
                        class="form-control"
                        id="searchInput"
                        placeholder="Search by description or URL"
                        aria-label="Search URLs"
                    >
                </div>
            </div>
            <div class="dashboard-actions">
                <div class="d-flex flex-wrap flex-md-nowrap align-items-center justify-content-md-end gap-2 header-actions">
                    <button
                        class="btn btn-outline-secondary d-none d-md-inline-flex"
                        type="button"
                        onclick="openManageCategoriesModal()"
                    >
                        <i class="fas fa-tags me-2"></i>Manage Categories
                    </button>
                    <button
                        class="btn btn-primary add-url-btn add-url-button"
                        data-bs-toggle="modal"
                        data-bs-target="#urlModal"
                        onclick="openAddModal()"
                    >
                        <i class="fas fa-plus me-2"></i><span>Add URL</span>
                    </button>
                    <div class="dropdown d-md-none">
                        <button
                            class="btn btn-outline-secondary dropdown-toggle action-menu-btn"
                            type="button"
                            id="headerActionsMenu"
                            data-bs-toggle="dropdown"
                            aria-expanded="false"
                        >
                            <i class="fas fa-ellipsis-h"></i>
                        </button>
                        <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="headerActionsMenu">
                            <li>
                                <button class="dropdown-item" type="button" onclick="openManageCategoriesModal()">
                                    <i class="fas fa-tags me-2"></i>Manage Categories
                                </button>
                            </li>
                            <li>
                                <button class="dropdown-item" type="button" onclick="handleLogout()">
                                    <i class="fas fa-sign-out-alt me-2"></i>Logout
                                </button>
                            </li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>

        <div class="row mb-4">
            <div class="col-12">
                <div class="d-flex flex-wrap align-items-center gap-2 category-filters" id="categoryFilters">
                    <span class="badge bg-light text-muted border">Loading categories...</span>
                </div>
            </div>
        </div>

        <div id="alertMessage"></div>

        <div id="urlsContainer" class="row">
            <div class="col-12 text-center py-5">
                <i class="fas fa-spinner fa-spin fa-3x text-muted"></i>
                <p class="mt-3 text-muted">Loading URLs...</p>
            </div>
        </div>

        <div id="emptyState" class="text-center py-5" style="display: none;">
            <i class="fas fa-link fa-4x text-muted mb-3"></i>
            <h4
                class="text-muted"
                id="emptyStateHeading"
                data-default-text='No URLs yet'
                data-search-text='No matching URLs'
            >No URLs yet</h4>
            <p
                class="text-muted"
                id="emptyStateSubheading"
                data-default-text='Click "Add URL" to create your first bookmark.'
                data-search-text='Try a different search term or add a new URL.'
            >Click "Add URL" to create your first bookmark.</p>
        </div>
    </div>

    <!-- Manage Categories Modal -->
    <div class="modal fade" id="manageCategoriesModal" tabindex="-1" aria-labelledby="manageCategoriesModalLabel">
        <div class="modal-dialog modal-dialog-scrollable">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="manageCategoriesModalLabel">Manage Categories</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <div id="categoriesModalAlert"></div>
                    <form id="addCategoryForm" class="row g-2 align-items-center">
                        <div class="col">
                            <label class="visually-hidden" for="newCategoryName">New Category Name</label>
                            <input
                                type="text"
                                class="form-control"
                                id="newCategoryName"
                                placeholder="Add a category"
                                maxlength="100"
                            >
                        </div>
                        <div class="col-auto">
                            <button type="submit" class="btn btn-primary">
                                <i class="fas fa-plus me-1"></i>Add
                            </button>
                        </div>
                    </form>
                    <hr>
                    <div class="d-flex justify-content-between align-items-center mb-2">
                        <h6 class="mb-0">Your Categories</h6>
                        <small class="text-muted" id="categoriesCount"></small>
                    </div>
                    <ul class="list-group" id="manageCategoriesList">
                        <li class="list-group-item text-muted text-center">Loading categories...</li>
                    </ul>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                </div>
            </div>
        </div>
    </div>

    <!-- URL Modal (for Add/Edit) -->
    <div class="modal fade" id="urlModal" tabindex="-1">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="modalTitle">Add New URL</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <div id="modalAlert"></div>
                    <form id="urlForm">
                        <input type="hidden" id="urlId">
                        <div class="mb-3">
                            <label for="description" class="form-label">Description</label>
                            <input type="text" class="form-control" id="description" required maxlength="500">
                        </div>
                        <div class="mb-3">
                            <label for="url" class="form-label">URL</label>
                            <input type="url" class="form-control" id="url" required>
                        </div>
                        <div class="mb-3">
                            <div class="d-flex justify-content-between align-items-center">
                                <label class="form-label mb-0" for="categoryCheckboxes">Categories</label>
                                <button
                                    type="button"
                                    class="btn btn-sm btn-outline-secondary"
                                    id="manageCategoriesFromModal"
                                >
                                    <i class="fas fa-tags me-1"></i>Manage
                                </button>
                            </div>
                            <div
                                id="categoryCheckboxes"
                                class="row gx-3 gy-2 mt-1 category-grid"
                                role="group"
                                aria-label="Choose categories for this URL"
                            >
                                <div class="col-12">
                                    <span class="text-muted small">No categories available yet.</span>
                                </div>
                            </div>
                            <div class="form-text">
                                Select one or more categories to help organize this link.
                            </div>
                        </div>
                    </form>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                    <button type="button" class="btn btn-primary" onclick="saveUrl()">
                        <i class="fas fa-save me-2"></i>Save
                    </button>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/main.js"></script>
    <script src="/js/dashboard.js"></script>
</body>
</html>

